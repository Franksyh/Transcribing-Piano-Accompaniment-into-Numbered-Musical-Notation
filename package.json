{
  "name": "piano-number-score-translator",
  "version": "1.0.0",
  "private": true,
  "description": "Local web tool for searching 91pu charts and generating piano numbered notation sheets.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/sw.js && node --check api/search.js && node --check api/song.js && node --check api/health.js && node --check api/_shared/pu.js && node --check netlify/functions/search.mts && node --check netlify/functions/song.mts && node --check netlify/functions/health.mts && node --check netlify/functions/_shared/pu.mts"
  }
}
