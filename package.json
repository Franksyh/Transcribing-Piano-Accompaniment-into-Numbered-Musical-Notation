{
  "name": "piano-number-score-translator",
  "version": "1.0.0",
  "private": true,
  "description": "Local web tool for searching 91pu charts and generating piano numbered notation sheets.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check netlify/functions/search.mts && node --check netlify/functions/song.mts && node --check netlify/functions/_shared/pu.mts"
  }
}
